import model from 'modules/gaji/gajiModel';

const { fields } = model;

export default [
  fields.karyawans,
  fields.periode,
  fields.tanggal,
  fields.karyawans,
  fields.proyeks,
  fields.catatan,
  fields.totalGaji,
];
